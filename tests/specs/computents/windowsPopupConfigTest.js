'use strict';

describe('Provider: windowsPopupConfig', function () {
  var windowsPopupConfig;

  // load the controller's module
  beforeEach(module('windowsPopupConfig'));


  // Initialize the controller and a mock scope
  beforeEach(inject(function ($injector) {
    windowsPopupConfig = $injector.get('windowsPopupConfig');
  }));


});

